#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
#define debug(x) (cerr << #x << ": " << (x) << endl)
const auto INF = numeric_limits<ll>::max();

int main() {
    ios_base::sync_with_stdio(false);
    ll n;
    cin >> n;
    vector<vector<ll>> wine(n, vector<ll>(n, 0));
    for (ll i = 0; i < n; i++)
        for (ll j = 0; j < n; j++)
            cin >> wine[i][j];

    
    
    
    return 0;
}

